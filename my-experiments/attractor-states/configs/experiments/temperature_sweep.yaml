name: temperature_sweep
description: "Vary sampling temperature. Tests robustness of attractor emergence under different levels of generation noise. High temperature = more random; does the attractor still emerge?"

instance_a:
  type: instruct_organism
  system_prompt: "You are a helpful assistant."

instance_b: same

protocol: standard
seeds: default

temperature: [0.3, 0.7, 1.2]   # 0.7 is the current default; sweep at submission time
# Note: top_p held fixed at 0.9
