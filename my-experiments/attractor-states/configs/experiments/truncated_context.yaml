name: truncated_context
description: "Each instance only sees its last N turns of history rather than the full conversation. Tests whether the attractor depends on long-range memory or emerges locally from recent context."

instance_a:
  type: instruct_organism
  system_prompt: "You are a helpful assistant."

instance_b: same

protocol: truncated       # truncate_to_last_n_turns: 6 — see protocols/truncated.yaml
seeds: default
